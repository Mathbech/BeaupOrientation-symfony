{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1>Add a new Course</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Add Course</button>
    {{ form_end(form) }}
{% endblock %}
